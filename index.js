/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MovieHome from './MovieHome';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MovieHome);
